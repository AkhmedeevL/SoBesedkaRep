<Window x:Class="SoBesedkaApp.UsersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SoBesedkaApp"
        mc:Ignorable="d"
        Title="Пользователи" Height="470.527" Width="500">
    <Grid Margin="0,0,3,0">
        <ListBox Name="listBoxUsers" HorizontalAlignment="Left" Margin="10,10,0,10" VerticalAlignment="Stretch" Width="290" ItemsSource="{Binding Users}">
            <ListBox.ItemContainerStyle>
                <Style TargetType="{x:Type ListBoxItem}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Path=isAdmin}" Value="True">
                            <Setter Property="Content" Value="{Binding Path=UserFIO}"/>
                            <Setter Property="ContentStringFormat" Value="{}{0} (Администратор)"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
        <Button x:Name="AdminButton" Content="Сделать администратором" HorizontalAlignment="Left" Height="46" Margin="314,10,0,0" VerticalAlignment="Top" Width="166" Click="AdminButton_Click"/>
        <Button x:Name="DeleteButton" Content="Удалить" HorizontalAlignment="Left" Margin="314,70,0,0" VerticalAlignment="Top" Width="164" Height="46" Click="DeleteButton_Click"/>

    </Grid>
</Window>
